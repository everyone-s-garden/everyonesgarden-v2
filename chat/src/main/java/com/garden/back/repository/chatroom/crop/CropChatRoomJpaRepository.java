package com.garden.back.repository.chatroom.crop;

import com.garden.back.domain.crop.CropChatRoom;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CropChatRoomJpaRepository extends JpaRepository<CropChatRoom, Long> {
}
