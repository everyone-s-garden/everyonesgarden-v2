package com.garden.back.crop.repository.chatroom;

import com.garden.back.crop.domain.CropChatRoom;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CropChatRoomJpaRepository extends JpaRepository<CropChatRoom, Long> {
}
